{
  "name": "bondhealth",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && node server.js",
    "dev": "nodemon --watch src --ext js --exec 'npm run build && node server.js'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}